const { Playlist } = require("./playlistModel");
const { Song } = require("./songModel");
const { User } = require("./userModel");
const { Artist } = require("./artistModel");

module.exports = { Playlist, Song, User, Artist };
